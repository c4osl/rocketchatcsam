diff --git a/node_modules/@rocket.chat/apps-compiler/compiler/AppsEngineValidator.js b/node_modules/@rocket.chat/apps-compiler/compiler/AppsEngineValidator.js
index c388e11..73f3922 100644
--- a/node_modules/@rocket.chat/apps-compiler/compiler/AppsEngineValidator.js
+++ b/node_modules/@rocket.chat/apps-compiler/compiler/AppsEngineValidator.js
@@ -97,8 +97,11 @@ class AppsEngineValidator {
             RealApp = mainClassModule;
         }
         else {
+            const exportedKeys = Object.keys(mainClassModule || {});
             RealApp =
-                mainClassModule?.default ?? mainClassModule[mainClassFile];
+                mainClassModule?.default ??
+                mainClassModule[mainClassFile] ??
+                (exportedKeys.length === 1 ? mainClassModule[exportedKeys[0]] : undefined);
         }
         if (!RealApp) {
             throw new Error(`There must be an exported class "${mainClassFile}" or a default export in the main class file.`);
@@ -137,10 +140,7 @@ class AppsEngineValidator {
             },
             exports,
         });
-        const result = vm.runInContext(compilationResult.files[`${filename}.js`].compiled, context);
-        if (Object.keys(exports).length === 1) {
-            return result;
-        }
+        vm.runInContext(compilationResult.files[`${filename}.js`].compiled, context);
         return exports;
     }
 }
