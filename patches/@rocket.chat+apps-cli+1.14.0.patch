diff --git a/node_modules/@rocket.chat/apps-cli/lib/misc/folderDetails.js b/node_modules/@rocket.chat/apps-cli/lib/misc/folderDetails.js
index 28eec0b..48e6fb8 100644
--- a/node_modules/@rocket.chat/apps-cli/lib/misc/folderDetails.js
+++ b/node_modules/@rocket.chat/apps-cli/lib/misc/folderDetails.js
@@ -82,10 +82,14 @@ class FolderDetails {
         if (!this.info) {
             throw new Error('App Manifest not loaded. Exiting...');
         }
-        if (!await this.doesFileExist('package.json')) {
+        let packageJsonPath = path.join(this.folder, 'package.json');
+        if (!await this.doesFileExist(packageJsonPath)) {
+            packageJsonPath = path.join(this.folder, '..', 'package.json');
+        }
+        if (!await this.doesFileExist(packageJsonPath)) {
             throw new Error('package.json not found. Exiting...');
         }
-        const packageJson = require(path.join(this.folder, 'package.json'));
+        const packageJson = require(packageJsonPath);
         const appsEngineEntry = packageJson.devDependencies['@rocket.chat/apps-engine'];
         if (!appsEngineEntry) {
             throw new Error('Make sure @rocket.chat/apps-engine is saved in your dev dependencies');
@@ -95,8 +99,7 @@ class FolderDetails {
             appsEngineEntry;
         if ((0, semver_1.diff)((0, semver_1.coerce)(appsEngineVersion), (0, semver_1.coerce)(this.info.requiredApiVersion))) {
             // tslint:disable-next-line:no-console
-            console.log(chalk_1.default.bgYellow('Warning:'), chalk_1.default.yellow('Different versions of the Apps Engine were found between app.json (', this.info.requiredApiVersion, ') and package.json (', appsEngineVersion, ').', '\nUpdating app.json to reflect the same version of Apps Engine from package.json'));
-            await this.updateInfoFileRequiredVersion(appsEngineVersion);
+            console.log(chalk_1.default.bgYellow('Warning:'), chalk_1.default.yellow('Different versions of the Apps Engine were found between app.json (', this.info.requiredApiVersion, ') and package.json (', appsEngineVersion, ').', '\nNot touching app.json: requiredApiVersion is a deliberate minimum, not a mirror of the devDependency version.'));
         }
     }
     validateAppDotJson() {
